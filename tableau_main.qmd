---
title: "Tableau training"
execute: 
  freeze: false
listing:
  contents:
    - "tableau/*.qmd"
  type: table
  categories: true
  fields: [title, categories]
---
  
```{r}
library(readr)
library(dplyr)
library(stringr)

cat("## Tableau training schedule")
```

```{r}
KINDR::training_sessions(tr_type = "Tableau", output_type = "DT")
```


